# Streak edge-case playthrough: `streak_about_to_break` vs `max_streak`

## Scope
- Mode: FAST WORKER deterministic run
- Presets tested in order: `streak_about_to_break`, then `max_streak`
- Validation targets: streak display load, milestone/protection surface load, store-vs-UI comparison

## Evidence summary
- URLs loaded successfully:
  - `http://127.0.0.1:5173/?skipOnboarding=true&devpreset=streak_about_to_break`
  - `http://127.0.0.1:5173/?skipOnboarding=true&devpreset=max_streak`
- Screenshots captured:
  - `/tmp/streak_about_to_break-edge-check.png`
  - `/tmp/max_streak-edge-check.png`
- Runtime readouts were pulled from:
  - UI text (`document.body.innerText`)
  - `localStorage.terra_save`
  - `window.__terraDebug()`

## Store vs UI comparison
| Preset | Screen at capture | UI streak text | UI due-review text | `stats.currentStreak` | `stats.bestStreak` | `streakFreezes` | `lastStreakMilestone` | `claimedMilestones` | `streakProtected` |
|---|---|---|---|---:|---:|---:|---:|---|---|
| `streak_about_to_break` | `cutscene` | `14 day streak` | `Due for Review 25` | 14 | 14 | 0 | 0 | `[]` | `false` |
| `max_streak` | `base` | `100 day streak` | `Due for Review 30` | 100 | 100 | 3 | 100 | `[3,7,14,30,60,100]` | `false` |

## Findings
1. **PASS**: streak display loads for both presets and matches save data (`stats.currentStreak`).
2. **PASS**: streak-related save fields are populated and distinct between edge and max presets (`streakFreezes`, `lastStreakMilestone`, `claimedMilestones`).
3. **FAIL / Coverage gap**: no user-facing milestone/protection surface could be reached in this DOM-driven run.
   - Text scan after load and after opening DEV panel had no milestone/protection UI labels (only streak headline, plus dev preset names such as "Streak at Risk" and "Streak Just Claimed").
   - Available DOM navigation was limited to cutscene skip + dev controls; primary in-game navigation appears Phaser-canvas driven and not directly reachable through deterministic DOM interaction.

## Pass/fail verdict
- **Overall: FAIL (partial)**
- Reason: streak values/display validated, but milestone/protection surfaces were not observable via reachable UI route in this run.
