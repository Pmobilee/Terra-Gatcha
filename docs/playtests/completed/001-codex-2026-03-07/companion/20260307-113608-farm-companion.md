# Companion/Farm Playthrough — `first_pet` + `five_rooms`

- Date (UTC): 2026-03-07 11:36:08
- Mode: FAST WORKER, breadth-first
- Runtime: Playwright (headless Chromium with `--no-sandbox`), mobile viewport 390x844

## Top Findings

1. **UI access gap (both presets):** No visible DOM testids/buttons for farm/companion/zoo on initial load (`testidCount=0`; only `Skip`, `DEV` buttons visible), so companion/farm screens were not directly reachable via normal DOM controls in this run.
2. **Routing mismatch on preset boot:** Both presets started on `currentScreen: "cutscene"` even with `?skipOnboarding=true&devpreset=...`, which blocked normal flow to base-room interactions.
3. **Companion state consistency passes:**
   - `first_pet`: `activeCompanion="trilobite"`, revived fossils include `trilobite`, farm empty (`[null,null,null]`).
   - `five_rooms`: `activeCompanion="trilobite"`, farm slot 0 contains trilobite, unlocked rooms include `farm` and `zoo`.
4. **Companion rendering evidence passes:** For both presets, forced `currentScreen='zoo'` rendered `The Zoo` and page text included `Trilobite`.
5. **Interaction attempt passes (at least one):** On `five_rooms` forced farm screen, `Collect All` succeeded: `dust 5000 -> 5016`, `lastCollectedAt` advanced on slot 0; remove flow was also triggered (attempted).

## Preset Results

### `first_pet`
- Initial state evidence: `currentScreen="cutscene"`; `activeCompanion="trilobite"`; `farm.slots=[null,null,null]`; `unlockedRooms=[command,lab,workshop]`.
- Accessibility: no farm/companion/zoo testid/button detected from initial UI.
- Farm screen: forced open for state verification; rendering present in follow-up check (`The Farm`, `Place Companion` text visible), but no successful slot action captured in this preset.
- Zoo screen: rendered with `The Zoo`; trilobite text present.

### `five_rooms`
- Initial state evidence: `currentScreen="cutscene"`; `activeCompanion="trilobite"`; slot 0 occupied by trilobite; `unlockedRooms` includes `farm` and `zoo`.
- Accessibility: no farm/companion/zoo testid/button detected from initial UI.
- Farm screen: forced open; rendered correctly (`The Farm`, companion/farm content visible).
- Interaction: `Collect All` updated farm/store values consistently (`dust +16`, slot `lastCollectedAt` updated).
- Zoo screen: rendered with `The Zoo`; trilobite text present.

## Pass/Fail

- **Overall: PASS (with UI access gap noted)**
- Functional goals met via state-driven access: companion/farm screens opened, companion rendering verified, and farm interaction + store consistency verified.
- **Gap to track:** missing discoverable farm/companion DOM route/testid from initial preset state; cutscene-first routing complicates direct UI playthrough.
