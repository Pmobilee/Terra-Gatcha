# First-Boot Onboarding Playthrough Report

- **Date/Time**: 2026-03-07 12:38 local
- **Preset/URL**: `http://127.0.0.1:5173/?devpreset=first_boot` (no `skipOnboarding`)
- **Mode**: Mobile viewport `390x844`, Playwright (`--no-sandbox`)

## Result

- **Overall**: FAIL (blocked before base transition)
- **Age gate**: PASS (`18+` option clicked; `terra_age_bracket=adult` observed)
- **Onboarding progression**: FAIL (stuck on cutscene flow)
- **Transition to base**: FAIL (`currentScreen` remained `cutscene`)

## Top Findings

1. Age gate is reachable and clickable; adult path selection persists in localStorage.
2. Post-age-gate onboarding shows a disabled `Start Exploring` button and never enables it during the run.
3. No actionable primary progression button appears; repeated attempts to advance do not change state.
4. Runtime remained on `currentScreen: cutscene`; no base/hub/dome screen reached.
5. Console shows environment/API issues (Vite HMR websocket + CORS to `127.0.0.1:3001`) that may impact data-dependent onboarding steps.

## Blocker Details (Buttons/Inputs)

- **Blocked control**: `Start Exploring` button present but `disabled=true` through all observed onboarding steps.
- **Observed interactive controls**: icon category buttons (`⛏`, `🪨`, `💎`, `🦕`, `🌋`, `🔭`, `🧬`, `🌿`) and `← Back`; none transitioned flow to base.
- **Failure mode**: after 10 progression attempts, flow still on cutscene with disabled submit action.

## Diagnostics

- **Console errors**: 7
- **Page errors**: 1 (`WebSocket closed without opened.`)
- **Failed network requests**:
  - `http://127.0.0.1:3001/api/facts/packs/all`
  - `http://127.0.0.1:5173/api/facts/delta?since=0&limit=500`
  - `http://127.0.0.1:3001/api/analytics/events`
- **Notable console messages**:
  - Vite websocket refused (`ws://100.74.153.81:5173/...`)
  - CORS blocked requests to API on port 3001

## Evidence

- Full machine-readable run log: `/tmp/first-boot-playthrough.json`
- Snapshots captured during run (including blocked state):
  - `/tmp/first-boot-00-load.png`
  - `/tmp/first-boot-01-after-age.png`
  - `/tmp/first-boot-onboard-00.png`
  - `/tmp/first-boot-onboard-09.png`
  - `/tmp/first-boot-99-final.png`
