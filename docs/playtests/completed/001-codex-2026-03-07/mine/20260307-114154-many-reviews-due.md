# Mine Playthrough - many_reviews_due (Run 2)

- Timestamp (UTC): 2026-03-07T11:41:31Z
- Runtime: Playwright Chromium (`--no-sandbox`), mobile viewport 390x844
- Start URL: `http://localhost:5173?skipOnboarding=true&devpreset=many_reviews_due`
- No code edits performed.

## Goal Coverage

1. Validate quiz frequency and due-card selection
2. Attempt at least 2 quiz interactions, including one wrong answer
3. Capture O2/state deltas and console errors

## Execution Notes

- Primary mine entry control was blocked in automated DOM flow (`[data-testid="btn-dive"]` unavailable), marked as a high-severity interaction defect.
- Continued run via direct GameManager fallback (`startDive(1)` + `forceQuiz()`) to complete quiz validation.
- Preset context reflected a due-heavy review set at prompt time (`30/30` due initially).

## Quiz Interactions

### Interaction 1 (Wrong answer)
- Fact ID: `cult-010`
- Source: `random`
- Fact had review state: `true`
- Fact due at prompt: `true`
- Chosen vs correct:
  - Chosen: `Workers operating dangerous machinery at close range`
  - Correct: `A train arriving at a station`
- Wrong flow confirmation: `Got it` button appeared (`true`)
- O2 delta: `2 -> 2` (`0`)

### Interaction 2 (Correct answer)
- Fact ID: `lsci-002`
- Source: `random`
- Fact had review state: `true`
- Fact due at prompt: `true`
- Chosen vs correct:
  - Chosen: `Low water content, high acidity, and hydrogen peroxide production`
  - Correct: `Low water content, high acidity, and hydrogen peroxide production`
- Wrong flow confirmation: `Got it` button appeared (`false`)
- O2 delta: `2 -> 2` (`0`)

## State Deltas

- Start state: screen `cutscene`, O2 `3`, due reviews `30/30`
- End state: screen `mining`, O2 `2`
- Due-review pool during interactions: `30/30` then `29/30` (one due card appears to have been advanced)

## Console / Runtime Errors

- Console errors observed:
  - Vite HMR websocket connection refused (`ws://100.74.153.81:5173/...`)
  - Resource load errors: HTTP `500`, `404`, `400`
- Page errors observed:
  - `WebSocket closed without opened.`

## Outcome

- Quiz interaction target met: **2 interactions completed, including 1 wrong answer**.
- Due-card selection evidence: **both prompted facts were due and had review states**.
- Overall result: **FAIL** (high-severity mine-entry interaction defect present in primary control path).
